import type { RewriteRouteOption } from "qwik-speak";

/** Translation paths */
export const rewriteRoutes: RewriteRouteOption[] = [
  {
    prefix: "es-AR",
    paths: {
      lads: "muchachos",
    },
  },
];
